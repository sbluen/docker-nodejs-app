generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model definitions {
  row_id  Int    @id @default(autoincrement())
  term    String
  meaning String
}
